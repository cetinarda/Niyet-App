node_modules
dist
.env
.env.local
dist.zip
dist_deploy.zip

# build-cities source cache (107 MB raw GeoNames JSON; regenerable from upstream)
.cache

# iOS
ios/App/Pods
ios/App/App.xcworkspace/xcuserdata
ios/App/DerivedData
*.xcuserstate

# Claude Code (kişisel ayarlar dışında — proje slash komutları repoda kalsın)
.claude/*
!.claude/commands/

# Background agent çıktıları — geçici raporlar, kullanıcıya gönderilir, repo'da yer tutmaz
agent_logs/

# DERLEME CIKTILARI (uretilir, commit edilmez)
# gdkpd dalinda bunlar bir donem izlenmisti: 409 dosya, 223 MB. Kaynak
# degisince yeniden uretiliyorlar, repoda tutmanin faydasi yok.
android/app/src/main/assets/public/   # npx cap sync android ciktisi
apps/soulid/.next/                    # next build ara ciktisi
apps/soulid/out/                      # next static export
