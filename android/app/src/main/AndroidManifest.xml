<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Meta App Events (Facebook SDK) — App ID/Client Token developers.facebook.com'dan. -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />
        <meta-data
            android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
            android:value="true" />
        <!-- iOS ile TUTARLI: reklam kimliği (GAID) toplanmıyor. Android'de ATT
             karşılığı yok ama Play Data Safety beyanını basit tutmak ve iki
             platformda aynı gizlilik hikâyesini anlatmak için kapalı.
             App Events ve SKAdNetwork/Play install atfı etkilenmez. -->
        <meta-data
            android:name="com.facebook.sdk.AdvertiserIDCollectionEnabled"
            android:value="false" />
        <!-- Anlık bildirim (FCM): bildirim çubuğu simgesi + vurgu rengi. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_sakin" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/sakin_notif" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: sakin://baglan vb. (App.jsx DEEP_LINK_SCREENS) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sakin" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- BİLDİRİM GÜVENİLİRLİĞİ (kullanıcı geri bildirimi: Android'de hatırlatmalar
         tek tek farklı saatlerde gelmesi gerekirken telefon uyanınca 5-6 tanesi
         birden düşüyordu). Kök sebep: exact-alarm izni olmadan Android 12+
         alarmları "yaklaşık" kuruyor, Doze + OEM pil optimizasyonu bunları
         biriktirip toplu teslim ediyor.
         SCHEDULE_EXACT_ALARM/USE_EXACT_ALARM BİLEREK EKLENMEDİ (geri alındı):
         Play Console'un "Tam Alarmlar" beyanı bu izni SADECE "Çalar saat" veya
         "Takvim" temel işlevli uygulamalara izin veriyor — Sakin ikisi de değil.
         Yanlış kategori seçip yayınlamak politika ihlali/askıya alınma riski
         taşır. @capacitor/local-notifications (TimedNotificationPublisher.java)
         izin yoksa zaten GRACEFUL DEĞRADE ediyor: canScheduleExactAlarms()
         false ise alarmManager.set() (yaklaşık) kullanıyor, crash olmuyor —
         sadece "tam saatinde" garantisi zayıflıyor. POST_NOTIFICATIONS (Android
         13+ çalışma-zamanı izni) + boot sonrası yeniden planlama (reboot'ta
         bildirimler kaybolmasın) yeterli ve politika-güvenli. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- REKLAM KİMLİĞİ (AD_ID) BİLEREK KALDIRILDI. Facebook SDK'nın kendi kütüphane
         manifesti bu izni OTOMATİK ekliyor — AdvertiserIDCollectionEnabled=false
         (yukarıda) sadece SDK'ya kimliği KULLANMAMASINI söylüyor, izni manifestten
         SİLMİYOR. Play Console'un "Reklam kimliği kullanıyor mu?" sorusu gerçek
         AAB'yi tarıyor; niyet ile beyan arasında fark olursa sorun çıkarır.
         Bu satır izni gerçekten kaldırır — "Hayır" cevabı GERÇEĞİ yansıtır,
         iOS'taki IDFA kapatmasıyla tutarlı. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
</manifest>
